Place your background image file here with the filename `auth-bg.png`.

Path: frontend/public/auth-bg.png

Then restart or rebuild the frontend:

# Run dev server
npm run dev

# Or build for production
npm run build

If you prefer another filename, update `background-image: url('/auth-bg.png')` in `frontend/src/styles.css` to match the path.